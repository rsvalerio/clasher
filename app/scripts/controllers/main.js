'use strict';

angular.module('clasherApp')
  .controller('MainCtrl', function ($scope, $rootScope, syncData, simpleLogin) {
    
    // $scope.profile = {
    //   user: {
    //     name: 'Rodrigo Valerio'
    //   }
    // };
    // $scope.profile = simpleLogin.profile;
    // $scope.logout = function () {
    //   simpleLogin.logout();
    // };
    // console.log($rootScope.profile);
    // syncData('/player').$bind($scope, 'teste');

  });
